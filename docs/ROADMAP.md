# 乐府 · 路线图

> 最后更新：2026-09-11 ｜ 当前版本：v1.0.2
> 关联文档：[DESIGN.md](../DESIGN.md)（UI 规范）、[CHANGELOG.md](../CHANGELOG.md)（已发布内容）

本路线图取代 DESIGN.md §9 的分期清单——那份清单的一期/二期/三期已基本落地（录制台、设置页、挂机监听、菜单栏、手动打点均已完成），剩下的空缺归入下方各版本继续推进。

## 总览

| 版本 | 主题 | 状态 | 一句话 |
|---|---|---|---|
| v1.0.2 | 安装链路加固与健壮性 | ✅ 已发布 | 修底层，不动界面 |
| **v1.0.3** | 分发可用性修复 | 🚧 进行中 | Intel 机器上 MP3 编码不可用 |
| **v1.1** | 界面美化 | 📋 规划中 | 把「封面即氛围」和卡拉OK 真正做出来 |
| **v1.2** | 多渠道分发 | 📋 规划中 | 让更多人、用更方便的方式拿到乐府 |
| **v1.3** | 多音源适配 | 📋 规划中 | 从「汽水专用」到「任意播放器」 |

排序理由：v1.0.3 是已发现的功能性缺陷，必须先修；v1.1 兑现既定设计承诺、纯收益无风险；v1.2 需要证书与外部平台投入，周期长，但决定触达面；v1.3 改动面最小、想象空间最大，放在最后做扎实。

---

## v1.0.3 — 分发可用性修复

已发布包是「通用二进制」，但内置的 `libmp3lame.0.dylib` 是从 Apple Silicon 的 Homebrew 直接拷来的 arm64 单架构，Intel 机器 `dlopen` 会报 `incompatible architecture`，MP3 编码静默回落到 M4A——用户以为存的是 MP3。

| # | 事项 | 说明 |
|---|---|---|
| [#13](https://github.com/boxter007/lefu/issues/13) | 内置 LAME 补齐双架构 | 用 `lipo` 合并 arm64 + x86_64，或 CI 里两架构分别编译后合并；打包脚本加一道「dylib 架构必须匹配宿主包架构」的校验，防止再退化 |
| — | CI 加架构自检 | 发布前断言 `lipo -archs` 与主程序一致，不一致直接 fail，不再靠人工发现 |
| — | 引导文案兜底 | 万一编码器不可用，设置页「引擎（高级）」的自检应把 LAME 标红并给出手动安装指引，而不是静默回落 |

---

## v1.1 — 界面美化

DESIGN.md 把「封面即氛围」和「卡拉OK 逐字」写成了设计原则，但代码里都只做了个意思。这一版把它们兑现，并把成品从「一次性产出」变成可回味的资产。

| # | 事项 | 现状 / 落点 |
|---|---|---|
| 1 | **封面驱动主题取色** | 现在 `RecordView.swift` 的 `artColor` 只是把 `track.key` 哈希进 `[accent, live, ok]` 三色表——跟封面毫无关系。改为真从 `session.artworkImage` 提取主色（`CIImage` + `CIAreaAverage`，或轻量 k-means），按 `track.key` 缓存；灰阶/低饱和封面回落默认粉。取色结果带动强调色、波形 active 柱、REC 呼吸点、状态徽章、背景光晕 |
| 2 | **卡拉OK 逐字高亮** | `SodaLyrics.krcToLrc` 现在把 KRC 的 `<偏移,长>` 词段信息直接丢掉，降级成整行 LRC，所以歌词只能整行切换。改为保留词段时间轴，按刻逐字点亮（当前行其余字半透明） |
| 3 | **曲库页（封面墙 + 内置播放）** | DESIGN.md §2 一直挂着「占位，v2 再启用」，实际上连占位页都没有，导航只有采诗/设置/指南。改为可用：按专辑/日期分组的封面墙、点击即播、右键重裁 / 转格式 / 在访达显示。这是把「府库」名副其实 |
| 4 | **视觉层次打磨** | 标题栏 `regularMaterial` + 侧轨 `ultraThinMaterial` 的材质关系再收一层；补齐空态与加载态；深浅双主题逐项过一遍对比度（正文 ≥ 4.5:1，辅助文字 ≥ 3:1） |
| 5 | **动效收口** | 对照 DESIGN.md §5 逐条核：换歌滑入、歌词整行过渡、停止按钮进度态、完成通知。缺的补上，多余的砍掉 |

---

## v1.2 — 多渠道分发

乐府目前只有两个获取入口：GitHub Releases（国内不稳）和 Homebrew（面向开发者）。这一版把「拿到」这件事做顺。

| # | 事项 | 说明 |
|---|---|---|
| 1 | **国内可用下载通道** | 落地页直连下载 + 展示 sha256；或接国内对象存储/CDN。目标：国内用户不翻墙也能装 |
| 2 | **应用内更新检查** | [#3](https://github.com/boxter007/lefu/issues/3) — 启动/菜单栏检查新版本，提示并一键跳转或下载 |
| 3 | **正式签名与公证** | [#2](https://github.com/boxter007/lefu/issues/2) — Developer ID 证书 + Apple 公证，消除 Gatekeeper 拦截。**同时解锁 hardened runtime**（v1.0.2 因 ad-hoc 会打断 `dlopen` 而刻意未启用，见 CHANGELOG） |
| 4 | **Mac App Store 可行性评估** | 沙盒限制与内录、MediaRemote 私有框架、屏幕录制权限存在冲突，需先做技术验证再决定；若不可行，结论写进文档并转向 Setapp / 独立分发 |
| 5 | **英文 README 与英文落地页** | [#1](https://github.com/boxter007/lefu/issues/1) — 覆盖海外用户，配套英文截图 |
| 6 | **分发度量** | Releases 下载量、brew 安装量、落地页转化——让「发布 → 触达」有数可看 |

---

## v1.3 — 多音源适配

乐府的内录链路本身与平台无关（BlackHole 抓的是系统声音，静音切分也是纯音频判断），唯一的耦合点只有一处硬编码。

| # | 事项 | 说明 |
|---|---|---|
| 1 | **音源平台白名单可配置** | `NowPlaying.swift` 里 `sodaBundle = "com.soda.music"` 写死。改为可配置，支持网易云 / QQ音乐 / Apple Music / Spotify / 哔哩哔哩等。**默认仍保持「仅汽水」**，避免误录到视频/播客 |
| 2 | **歌词源分层扩展** | 汽水走本地 KRC（现状）；其他平台走 LRCLIB → 网易云（`Lyrics.swift` 已有这条路）。补各平台歌词接口与逐字格式映射 |
| 3 | **跨平台切歌与静音切分协同** | 非汽水下没有 KRC 边界信息，切歌判断需靠 MediaRemote 事件 + `SilenceSplitter`，前奏长的歌 / 电台混播场景要单独调参验证 |
| 4 | **来源标注与命名模板** | 成品标签与文件名可含来源平台；实现 DESIGN.md 三期的命名模板（`{歌手} - {歌名}` / `{日期} {歌名}` / `{专辑}`） |

---

## 候选 / 待议

尚未排期，也可能穿插进上面任一版本：

- **音频路由 CFString 泄漏**：`AudioRouting` 中 `AudioObjectGetPropertyData` 读 `kAudioObjectPropertyName` / `DeviceUID` 返回的 CFString 是 +1 引用，代码从不释放，每次调用漏一个
- **批量格式转换**（DESIGN.md 二期）：MP3 / M4A / WAV 互转，右键或拖拽
- **崩溃恢复完整链路**（DESIGN.md 一期）：目前只清理孤儿 session 目录，尚缺「有 WAV 无成品 → 提示一键重裁」
- **曲目元数据编辑**：成品库里直接改歌名/歌手/封面并回写标签

## 已完成（存档）

| 版本 | 内容 |
|---|---|
| v1.0.0 | 首个公开版本：采诗、裁曲、逐首录制、四路歌词、挂机监听、菜单栏 |
| v1.0.1 | 长时间挂机性能病、列表定位与波形渲染回归、版本号体系 |
| v1.0.2 | 安装包双重校验、日志容量上限、签名流程规范化、Homebrew cask 自动化 |
